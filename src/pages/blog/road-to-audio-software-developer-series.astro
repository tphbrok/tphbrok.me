---
import Layout from "../../layouts/Layout.astro";
import { getCollection } from "astro:content";
import dayjs from "dayjs";

const posts = (await getCollection("blog"))
    .filter((post) => post.id.includes("road-to-audio-software-developer-"))
    .sort(
        (a, b) => b.data.dateCreated.valueOf() - a.data.dateCreated.valueOf()
    );
---

<Layout>
    <h1>Blog series: 'The Road To Becoming An Audio Software Developer'</h1>

    In this series, I explore the audio software development ecosystem, with all
    its related fields such as music production, signal processing and systems
    programming.

    <ul class="mt-8">
        {
            posts.map((post) => (
                <li>
                    <a href={`/blog/${post.id}/`}>
                        <h4 class="title">{post.data.title}</h4>
                    </a>
                    <span class="italic text-sm">
                        published{" "}
                        {dayjs(post.data.dateCreated).format("MMMM D, YYYY")}
                    </span>
                    <p class="indent-0">{post.data.description}</p>
                </li>
            ))
        }
    </ul>
</Layout>
